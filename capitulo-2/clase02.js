//STRINGS

let nombre = 'juan', apellido = 'perez'

let nombreMayuscula = nombre.toUpperCase()
let nombreMinuscula = nombre.toLocaleLowerCase()

let primerLetra = nombre.charAt(0)
let cantidad = nombre.length

let nombreCompleto = `${nombre} ${apellido}`

let str = nombre.substring(1, 3)

let ultimaLetra = nombre.charAt(nombre.length - 1)